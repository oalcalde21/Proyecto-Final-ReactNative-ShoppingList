import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    itemContainer: {
        margin: 5,
        padding: 5,
        borderRadius: 5,
        backgroundColor: "#fff",
        borderWidth: 1,
        borderColor: "red",
      },
      item: {
        padding: 10,
        textAlign: "center",
      },
  });